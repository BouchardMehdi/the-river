{
  "name": "the-river",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix back run build",
    "start": "node dist/main.js"
  },
  "engines": {
    "node": "22.x"
  }
}
